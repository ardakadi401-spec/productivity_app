{
  "name": "firestore-rules-tests",
  "private": true,
  "version": "1.0.0",
  "description": "FAZ 15 — firestore.rules'un Firebase Emulator Suite ile otomatik doğrulaması (ROADMAP.md FAZ 15 Riskler: 'Firebase Emulator Suite ile Rules'un otomatik test senaryolarıyla doğrulanması zorunlu kılınır'). Flutter/Dart bağımlılıklarından tamamen izole, yalnızca bu klasörde yaşayan bir Node araç seti.",
  "type": "module",
  "scripts": {
    "test": "node --test rules.test.mjs"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0"
  }
}
